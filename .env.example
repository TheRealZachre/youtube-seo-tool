# Default demo channel (BeOne Medicines)
YOUTUBE_CHANNEL=@BeOneMedicines

# Optional: YouTube Data API v3 key for live channel + video ingest
# Without this key the app uses the BeOne seed library
YOUTUBE_API_KEY=

# Optional future enhancement (rule/template generation works without these)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
