# Location note

This monorepo currently lives under `youtube-seo-tool/video-clips` until a dedicated `TheRealZachre/video-clips` GitHub repository can be created. Treat `video-clips/` as the product root (`npm install` from this directory).
